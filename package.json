{
  "name": "qodfy-monorepo",
  "private": true,
  "version": "0.2.6",
  "description": "Open-source launch readiness scanner for AI-built apps.",
  "scripts": {
    "dev": "pnpm dev:web",
    "dev:web": "turbo dev --filter=@qodfy/web",
    "dev:cli": "pnpm --filter qodfy exec tsx src/index.ts scan --path ../../apps/web",
    "build": "turbo build",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.9.12",
    "typescript": "^6.0.3"
  },
  "packageManager": "pnpm@10.5.2"
}
